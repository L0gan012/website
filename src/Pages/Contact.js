import React from 'react';

import ContactForm from '../Components/ContactForm.js';

const Contact = () => {
    return (
        <section className='contact-section vert'>
            <h1 className='contact-header'>Contact Form</h1>
            <ContactForm/>
        </section>
    );
};

export default Contact;